apiVersion: v1
kind: Service
metadata:
  name: wordpress
  labels:
    name: wordpress
  namespace: blog
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
  selector:
    name: blog
