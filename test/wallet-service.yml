apiVersion: v1
kind: Service
metadata:
  name: wallet
  labels:
    name: wallet
spec:
  ports:
    - port: 3300
      targetPort: 3300
      protocol: TCP
  type: NodePort
  selector:
    name: wallet
